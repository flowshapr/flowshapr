{
  "name": "genkit-builder",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "server",
    "sdk"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=server",
    "start": "npm run start --workspace=frontend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=server",
    "type-check": "npm run type-check --workspace=frontend && npm run type-check --workspace=server",
    "restart": "./scripts/restart-frontend.sh",
    "restart:all": "npm run restart --workspace=server",
    "clean": "npm run clean --workspace=server",
    "mcp:playwright": "mcp-server-playwright --host 127.0.0.1 --port 8899 --output-dir .playwright-mcp --isolated",
    "testapps:install": "cd testapps/flowshapr-sdk-basic && npm install && cd ../genkit-client-compatibility && npm install && cd ../streaming-flows && npm install",
    "testapps:sdk-basic": "cd testapps/flowshapr-sdk-basic && npm test",
    "testapps:genkit-compat": "cd testapps/genkit-client-compatibility && npm test",
    "testapps:streaming": "cd testapps/streaming-flows && npm test",
    "testapps:all": "npm run testapps:sdk-basic && npm run testapps:genkit-compat && npm run testapps:streaming",
    "testapps": "npm run testapps:all"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^20"
  }
}